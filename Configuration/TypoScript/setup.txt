plugin.tx_placements {
	view {
		templateRootPath = {$plugin.tx_placements.view.templateRootPath}
		partialRootPath = {$plugin.tx_placements.view.partialRootPath}
		layoutRootPath = {$plugin.tx_placements.view.layoutRootPath}
		widget {
			Webfox\Placements\ViewHelpers\Widget\PaginateViewHelper {
				templateRootPath = {$plugin.tx_placements.view.templateRootPath}
			}
		}
	}
	persistence {
		storagePid = {$plugin.tx_placements.persistence.storagePid}
	}
	features {
		# uncomment the following line to enable the new Property Mapper.
		# rewrittenPropertyMapper = 1
	}

	settings {
		security.editorGroup =
		position {
			list {
				summary.crop = 35	
				# Paginate configuration.
				paginate {
					itemsPerPage ={$plugin.tx_placements.settings.list.paginate.itemsPerPage}
					insertAbove = {$plugin.tx_placements.settings.list.paginate.insertAbove}
					insertBelow = {$plugin.tx_placements.settings.list.paginate.insertBelow}
					lessPages = {$plugin.tx_placements.settings.list.paginate.lessPages}
					forcedNumberOfLinks = {$plugin.tx_placements.settings.list.paginate.forcedNumberOfLinks}
					pagesBefore = {$plugin.tx_placements.settings.list.paginate.pagesBefore}
					pagesAfter = {$plugin.tx_placements.settings.list.paginate.pagesAfter}
					templatePath =
					prevNextHeaderTags = {$plugin.tx_placements.settings.list.paginate.prevNextHeaderTags}
				}
			}
		}
	}
}

plugin.tx_placements._CSS_DEFAULT_STYLE (
	textarea.f3-form-error {
		background-color:#FF9F9F;
		border: 1px #FF0000 solid;
	}

	input.f3-form-error {
		background-color:#FF9F9F;
		border: 1px #FF0000 solid;
	}

	.tx-placements table {
		border-collapse:separate;
		border-spacing:10px;
	}

	.tx-placements table th {
		font-weight:bold;
	}

	.tx-placements table td {
		vertical-align:top;
	}
)
